import Homepage from "../Components/Homepage";

const presentationComponents = () => {
    return [
        {
            title: 'Homepage',
            component: <Homepage />
        }
    ];
};

export {presentationComponents}